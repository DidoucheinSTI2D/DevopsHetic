all:
  vars:
    ansible_connection: docker
    ansible_python_interpreter: /usr/bin/python3
    ansible_become: false
  children:
    webservers:
      hosts:
        ansible-node1:
    appservers:
      hosts:
        ansible-node2:
